# Admin UI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Actualizar el formulario de producto en admin para usar `attributes` (editor clave/valor dinámico), y crear la sección de gestión de promociones.

**Architecture:** `ProductFormModal.tsx` reemplaza los campos `specifications`, `volume_number`, `language`, `figure_scale`, `manufacturer`, `category` por un editor de pares clave/valor que escribe en `attributes`. Se crea una nueva página `app/admin/promotions/page.tsx` con su hook `useAdminPromotions.ts`.

**Tech Stack:** React 19, TypeScript, Tailwind CSS 4, Next.js 15 App Router

**Prerequisito:** Plan A (product-model-refactor) y Plan B (promotions-model) deben estar aplicados.

---

## Files

- Modify: `app/admin/products/ProductFormModal.tsx`
- Create: `app/admin/promotions/page.tsx`
- Create: `app/admin/promotions/useAdminPromotions.ts`
- Create: `app/admin/promotions/PromotionFormModal.tsx`
- Modify: `app/admin/page.tsx` (agregar link a promociones)

---

### Task 1: Actualizar `ProductFormModal.tsx` — reemplazar campos fijos por editor `attributes`

**Files:**
- Modify: `app/admin/products/ProductFormModal.tsx`

- [ ] **Step 1: Reemplazar el estado de `specs` por `attributes`**

Dentro del componente, eliminar el estado `specs` y las variables `showVolume`, `showLanguage`, `showFigureFields`, `showDemographic`. Agregar en su lugar:

```tsx
const [attributes, setAttributes] = useState<Record<string, string>>(
  Object.fromEntries(
    Object.entries(product?.attributes ?? {}).map(([k, v]) => [k, String(v)])
  )
);

function setAttr(key: string, value: string) {
  setAttributes((prev) => ({ ...prev, [key]: value }));
}

function addAttrRow() {
  setAttributes((prev) => ({ ...prev, '': '' }));
}

function removeAttrRow(key: string) {
  setAttributes((prev) => {
    const next = { ...prev };
    delete next[key];
    return next;
  });
}

function renameAttrKey(oldKey: string, newKey: string) {
  setAttributes((prev) => {
    const next: Record<string, string> = {};
    for (const [k, v] of Object.entries(prev)) {
      next[k === oldKey ? newKey : k] = v;
    }
    return next;
  });
}
```

- [ ] **Step 2: Actualizar `EMPTY` y la inicialización del formulario**

Reemplazar `EMPTY` por:

```tsx
const EMPTY: Partial<AdminProduct> = {
  title: '', sku: '', editorial: '', author: null, series: null,
  price_pen: 0, stock: 0, stock_status: 'in_stock',
  country_code: 'AR', type: 'manga',
  eta_text: null, description: null, full_description: null,
  images: [], tags: [], attributes: {}, is_active: true,
  estimated_arrival: null, preorder_deposit: undefined,
  series_status: null, demographic: null,
};
```

- [ ] **Step 3: Actualizar `handleSubmit` para incluir `attributes`**

En la función `handleSubmit`, antes de llamar `onSubmit`, construir los attributes finales:

```tsx
const parsedAttributes: Record<string, string | number | boolean> = {};
for (const [k, v] of Object.entries(attributes)) {
  if (!k.trim()) continue;
  const num = Number(v);
  if (!isNaN(num) && v.trim() !== '') {
    parsedAttributes[k.trim()] = num;
  } else if (v === 'true') {
    parsedAttributes[k.trim()] = true;
  } else if (v === 'false') {
    parsedAttributes[k.trim()] = false;
  } else {
    parsedAttributes[k.trim()] = v;
  }
}

const payload = { ...form, attributes: parsedAttributes };
await onSubmit(payload);
```

- [ ] **Step 4: Reemplazar la sección "Imágenes y especificaciones" en el JSX**

Eliminar el grid de specs y reemplazar por el editor dinámico:

```tsx
{/* Atributos dinámicos */}
<section>
  <h3 className="text-sm font-semibold text-gray-800 dark:text-gray-200 mb-3 pb-1 border-b border-gray-100 dark:border-gray-700">
    Atributos
  </h3>
  <div className="space-y-2">
    {Object.entries(attributes).map(([key, value], i) => (
      <div key={i} className="flex gap-2 items-center">
        <input
          className={inputClass + ' flex-1'}
          placeholder="clave (ej: volume, brand)"
          value={key}
          onChange={(e) => renameAttrKey(key, e.target.value)}
        />
        <input
          className={inputClass + ' flex-1'}
          placeholder="valor"
          value={value}
          onChange={(e) => setAttr(key, e.target.value)}
        />
        <button
          type="button"
          onClick={() => removeAttrRow(key)}
          className="text-red-400 hover:text-red-600 text-lg leading-none px-1"
        >
          ×
        </button>
      </div>
    ))}
    <button
      type="button"
      onClick={addAttrRow}
      className="text-sm text-[#2b496d] dark:text-blue-400 hover:underline"
    >
      + Agregar atributo
    </button>
  </div>
</section>
```

- [ ] **Step 5: Eliminar imports no usados**

Quitar de los imports: `CATEGORIES`, `LANGUAGES`, `LANGUAGE_LABELS` y cualquier referencia a `specifications`.

- [ ] **Step 6: Verificar build**

```bash
npm run build 2>&1 | tail -15
```

- [ ] **Step 7: Commit**

```bash
git add app/admin/products/ProductFormModal.tsx
git commit -m "feat: replace product specs/category fields with dynamic attributes editor"
```

---

### Task 2: Hook `useAdminPromotions.ts`

**Files:**
- Create: `app/admin/promotions/useAdminPromotions.ts`

- [ ] **Step 1: Crear el archivo**

```ts
'use client';

import { useState, useCallback, useEffect } from 'react';
import type { ToastType } from '@/hooks/useToast';
import type { Promotion } from '@/lib/promotions';

type ToastFn = (message: string, type: ToastType) => void;

export function useAdminPromotions(toast: ToastFn) {
  const [promotions, setPromotions] = useState<Promotion[]>([]);
  const [loading, setLoading] = useState(false);

  const fetch = useCallback(async () => {
    setLoading(true);
    const res = await globalThis.fetch('/api/admin/promotions');
    const json = await res.json();
    setLoading(false);
    if (!res.ok) { toast(json.error ?? 'Error al cargar promociones', 'error'); return; }
    setPromotions(json.data ?? []);
  }, [toast]);

  useEffect(() => { fetch(); }, [fetch]);

  const create = useCallback(async (data: Omit<Promotion, 'id' | 'usedCount'>) => {
    const res = await globalThis.fetch('/api/admin/promotions', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    const json = await res.json();
    if (!res.ok) { toast(json.error ?? 'Error al crear promoción', 'error'); return false; }
    toast('Promoción creada', 'success');
    fetch();
    return true;
  }, [toast, fetch]);

  const update = useCallback(async (id: string, data: Partial<Promotion>) => {
    const res = await globalThis.fetch(`/api/admin/promotions/${id}`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data),
    });
    const json = await res.json();
    if (!res.ok) { toast(json.error ?? 'Error al actualizar', 'error'); return false; }
    setPromotions((prev) => prev.map((p) => p.id === id ? { ...p, ...data } : p));
    toast('Promoción actualizada', 'success');
    return true;
  }, [toast]);

  const remove = useCallback(async (id: string) => {
    const res = await globalThis.fetch(`/api/admin/promotions/${id}`, { method: 'DELETE' });
    const json = await res.json();
    if (!res.ok) { toast(json.error ?? 'Error al eliminar', 'error'); return false; }
    setPromotions((prev) => prev.filter((p) => p.id !== id));
    toast('Promoción eliminada', 'success');
    return true;
  }, [toast]);

  const toggleActive = useCallback(async (id: string, active: boolean) => {
    return update(id, { active });
  }, [update]);

  return { promotions, loading, create, update, remove, toggleActive, refresh: fetch };
}
```

- [ ] **Step 2: Commit**

```bash
git add app/admin/promotions/useAdminPromotions.ts
git commit -m "feat: add useAdminPromotions hook"
```

---

### Task 3: Modal de promoción `PromotionFormModal.tsx`

**Files:**
- Create: `app/admin/promotions/PromotionFormModal.tsx`

- [ ] **Step 1: Crear el componente**

```tsx
'use client';

import { useState, type FormEvent } from 'react';
import { X, Loader2 } from 'lucide-react';
import type { Promotion } from '@/lib/promotions';
import { PRODUCT_TYPES, PRODUCT_TYPE_LABELS } from '@/lib/constants/productTypes';

interface Props {
  promotion?: Promotion | null;
  onClose: () => void;
  onSubmit: (data: Omit<Promotion, 'id' | 'usedCount'>) => Promise<boolean>;
}

const EMPTY: Omit<Promotion, 'id' | 'usedCount'> = {
  name: '',
  code: null,
  type: 'percent',
  value: 10,
  scope: 'global',
  productIds: null,
  productTypes: null,
  startDate: null,
  endDate: null,
  active: true,
  maxUses: null,
};

const inputClass = 'w-full px-3 py-2 text-sm rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-[#2b496d]';
const labelClass = 'block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1';

export default function PromotionFormModal({ promotion, onClose, onSubmit }: Props) {
  const isEdit = !!promotion;
  const [form, setForm] = useState<Omit<Promotion, 'id' | 'usedCount'>>(
    promotion
      ? {
          name: promotion.name,
          code: promotion.code ?? null,
          type: promotion.type,
          value: promotion.value,
          scope: promotion.scope,
          productIds: promotion.productIds ?? null,
          productTypes: promotion.productTypes ?? null,
          startDate: promotion.startDate ?? null,
          endDate: promotion.endDate ?? null,
          active: promotion.active,
          maxUses: promotion.maxUses ?? null,
        }
      : EMPTY,
  );
  const [saving, setSaving] = useState(false);
  const [productIdsInput, setProductIdsInput] = useState(
    (promotion?.productIds ?? []).join(', ')
  );

  function set<K extends keyof typeof form>(key: K, value: (typeof form)[K]) {
    setForm((prev) => ({ ...prev, [key]: value }));
  }

  async function handleSubmit(e: FormEvent) {
    e.preventDefault();
    setSaving(true);
    const payload = {
      ...form,
      productIds: form.scope === 'product'
        ? productIdsInput.split(',').map((s) => s.trim()).filter(Boolean)
        : null,
      productTypes: form.scope === 'product_type' ? form.productTypes : null,
      code: form.code?.trim() || null,
      maxUses: form.maxUses ?? null,
    };
    const ok = await onSubmit(payload);
    setSaving(false);
    if (ok) onClose();
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4">
      <div className="bg-white dark:bg-gray-900 rounded-2xl shadow-2xl w-full max-w-lg max-h-[90vh] flex flex-col">
        <div className="flex items-center justify-between px-6 py-4 border-b border-gray-200 dark:border-gray-700">
          <h2 className="text-lg font-bold text-gray-900 dark:text-white">
            {isEdit ? 'Editar promoción' : 'Nueva promoción'}
          </h2>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600">
            <X size={20} />
          </button>
        </div>

        <form onSubmit={handleSubmit} className="overflow-y-auto px-6 py-5 space-y-4">
          <div>
            <label className={labelClass}>Nombre *</label>
            <input className={inputClass} required value={form.name}
              onChange={(e) => set('name', e.target.value)} placeholder="Cyber Monday, Promo Julio…" />
          </div>

          <div className="grid grid-cols-2 gap-4">
            <div>
              <label className={labelClass}>Tipo *</label>
              <select className={inputClass} value={form.type} onChange={(e) => set('type', e.target.value as 'percent' | 'fixed')}>
                <option value="percent">Porcentaje (%)</option>
                <option value="fixed">Monto fijo (S/)</option>
              </select>
            </div>
            <div>
              <label className={labelClass}>Valor *</label>
              <input type="number" min="0.01" step="0.01" required className={inputClass}
                value={form.value} onChange={(e) => set('value', parseFloat(e.target.value))} />
            </div>
          </div>

          <div>
            <label className={labelClass}>Código de cupón (opcional)</label>
            <input className={inputClass} value={form.code ?? ''} placeholder="CYBER20"
              onChange={(e) => set('code', e.target.value.toUpperCase() || null)} />
            <p className="text-xs text-gray-500 mt-1">Solo si es un cupón que el cliente debe ingresar</p>
          </div>

          <div>
            <label className={labelClass}>Alcance *</label>
            <select className={inputClass} value={form.scope}
              onChange={(e) => set('scope', e.target.value as Promotion['scope'])}>
              <option value="global">Global (todos los productos)</option>
              <option value="product">Productos específicos</option>
              <option value="product_type">Tipo de producto</option>
            </select>
          </div>

          {form.scope === 'product' && (
            <div>
              <label className={labelClass}>IDs de productos (separados por coma)</label>
              <input className={inputClass} value={productIdsInput}
                onChange={(e) => setProductIdsInput(e.target.value)}
                placeholder="uuid1, uuid2, uuid3" />
            </div>
          )}

          {form.scope === 'product_type' && (
            <div>
              <label className={labelClass}>Tipos de producto</label>
              <div className="flex flex-wrap gap-2 mt-1">
                {PRODUCT_TYPES.map((t) => (
                  <label key={t} className="flex items-center gap-1.5 text-sm cursor-pointer">
                    <input type="checkbox"
                      checked={(form.productTypes ?? []).includes(t)}
                      onChange={(e) => {
                        const current = form.productTypes ?? [];
                        set('productTypes', e.target.checked
                          ? [...current, t]
                          : current.filter((x) => x !== t));
                      }} />
                    {PRODUCT_TYPE_LABELS[t]}
                  </label>
                ))}
              </div>
            </div>
          )}

          <div className="grid grid-cols-2 gap-4">
            <div>
              <label className={labelClass}>Fecha inicio</label>
              <input type="datetime-local" className={inputClass}
                value={form.startDate?.slice(0, 16) ?? ''}
                onChange={(e) => set('startDate', e.target.value || null)} />
            </div>
            <div>
              <label className={labelClass}>Fecha fin</label>
              <input type="datetime-local" className={inputClass}
                value={form.endDate?.slice(0, 16) ?? ''}
                onChange={(e) => set('endDate', e.target.value || null)} />
            </div>
          </div>

          <div>
            <label className={labelClass}>Máximo de usos (opcional)</label>
            <input type="number" min="1" className={inputClass}
              value={form.maxUses ?? ''}
              onChange={(e) => set('maxUses', e.target.value ? parseInt(e.target.value) : null)} />
          </div>

          <div className="flex items-center gap-2">
            <input type="checkbox" id="promo_active" checked={form.active}
              onChange={(e) => set('active', e.target.checked)}
              className="w-4 h-4 rounded border-gray-300 text-[#2b496d]" />
            <label htmlFor="promo_active" className="text-sm text-gray-700 dark:text-gray-300">
              Promoción activa
            </label>
          </div>
        </form>

        <div className="flex justify-end gap-3 px-6 py-4 border-t border-gray-200 dark:border-gray-700">
          <button type="button" onClick={onClose}
            className="px-4 py-2 text-sm rounded-lg border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700">
            Cancelar
          </button>
          <button type="submit" disabled={saving} onClick={handleSubmit as unknown as React.MouseEventHandler}
            className="px-5 py-2 text-sm rounded-lg bg-[#2b496d] hover:bg-[#1e3550] disabled:opacity-50 text-white font-semibold flex items-center gap-2">
            {saving && <Loader2 size={15} className="animate-spin" />}
            {isEdit ? 'Guardar cambios' : 'Crear promoción'}
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/admin/promotions/PromotionFormModal.tsx
git commit -m "feat: add PromotionFormModal component"
```

---

### Task 4: Página de admin de promociones

**Files:**
- Create: `app/admin/promotions/page.tsx`

- [ ] **Step 1: Crear la página**

```tsx
'use client';

import { useState } from 'react';
import { Plus, Pencil, Trash2, ToggleLeft, ToggleRight } from 'lucide-react';
import { useAdminPromotions } from './useAdminPromotions';
import PromotionFormModal from './PromotionFormModal';
import { useToast } from '@/hooks/useToast';
import type { Promotion } from '@/lib/promotions';

export default function PromotionsPage() {
  const { showToast, Toaster } = useToast();
  const { promotions, loading, create, update, remove, toggleActive } = useAdminPromotions(showToast);
  const [modalOpen, setModalOpen] = useState(false);
  const [editing, setEditing] = useState<Promotion | null>(null);

  function openCreate() { setEditing(null); setModalOpen(true); }
  function openEdit(p: Promotion) { setEditing(p); setModalOpen(true); }

  async function handleSubmit(data: Omit<Promotion, 'id' | 'usedCount'>) {
    if (editing) return update(editing.id, data);
    return create(data);
  }

  const scopeLabel = (p: Promotion) => {
    if (p.scope === 'global') return 'Global';
    if (p.scope === 'product') return `${(p.productIds ?? []).length} producto(s)`;
    return (p.productTypes ?? []).join(', ');
  };

  return (
    <div className="p-6 max-w-5xl mx-auto">
      <Toaster />
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold text-gray-900 dark:text-white">Promociones</h1>
        <button onClick={openCreate}
          className="flex items-center gap-2 px-4 py-2 bg-[#2b496d] hover:bg-[#1e3550] text-white text-sm font-semibold rounded-lg">
          <Plus size={16} /> Nueva promoción
        </button>
      </div>

      {loading ? (
        <p className="text-gray-500 text-sm">Cargando…</p>
      ) : promotions.length === 0 ? (
        <p className="text-gray-500 text-sm">No hay promociones creadas.</p>
      ) : (
        <div className="overflow-x-auto rounded-xl border border-gray-200 dark:border-gray-700">
          <table className="w-full text-sm">
            <thead className="bg-gray-50 dark:bg-gray-800 text-gray-600 dark:text-gray-400 text-xs uppercase">
              <tr>
                <th className="px-4 py-3 text-left">Nombre</th>
                <th className="px-4 py-3 text-left">Descuento</th>
                <th className="px-4 py-3 text-left">Alcance</th>
                <th className="px-4 py-3 text-left">Cupón</th>
                <th className="px-4 py-3 text-left">Usos</th>
                <th className="px-4 py-3 text-left">Estado</th>
                <th className="px-4 py-3" />
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-100 dark:divide-gray-700">
              {promotions.map((p) => (
                <tr key={p.id} className="bg-white dark:bg-gray-900 hover:bg-gray-50 dark:hover:bg-gray-800">
                  <td className="px-4 py-3 font-medium text-gray-900 dark:text-white">{p.name}</td>
                  <td className="px-4 py-3 text-purple-600 dark:text-purple-400 font-mono">
                    {p.type === 'percent' ? `${p.value}%` : `S/ ${p.value}`}
                  </td>
                  <td className="px-4 py-3 text-gray-600 dark:text-gray-400">{scopeLabel(p)}</td>
                  <td className="px-4 py-3 font-mono text-xs text-gray-500">{p.code ?? '—'}</td>
                  <td className="px-4 py-3 text-gray-600 dark:text-gray-400">
                    {p.usedCount}{p.maxUses ? ` / ${p.maxUses}` : ''}
                  </td>
                  <td className="px-4 py-3">
                    <button onClick={() => toggleActive(p.id, !p.active)}
                      className={p.active ? 'text-green-500' : 'text-gray-400'}>
                      {p.active ? <ToggleRight size={20} /> : <ToggleLeft size={20} />}
                    </button>
                  </td>
                  <td className="px-4 py-3 flex gap-2 justify-end">
                    <button onClick={() => openEdit(p)} className="text-gray-400 hover:text-[#2b496d]">
                      <Pencil size={15} />
                    </button>
                    <button onClick={() => remove(p.id)} className="text-gray-400 hover:text-red-500">
                      <Trash2 size={15} />
                    </button>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}

      {modalOpen && (
        <PromotionFormModal
          promotion={editing}
          onClose={() => setModalOpen(false)}
          onSubmit={handleSubmit}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verificar build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add app/admin/promotions/
git commit -m "feat: add promotions admin page with CRUD table and modal"
```

---

### Task 5: Agregar link a Promociones en el panel admin

**Files:**
- Modify: `app/admin/page.tsx`

- [ ] **Step 1: Leer el archivo actual**

```bash
cat app/admin/page.tsx | head -60
```

- [ ] **Step 2: Agregar card de navegación a `/admin/promotions`**

Buscar la sección donde están los links/cards del panel admin y agregar:

```tsx
<Link href="/admin/promotions"
  className="flex flex-col gap-2 p-5 rounded-xl border border-gray-200 dark:border-gray-700 hover:border-[#2b496d] bg-white dark:bg-gray-900 transition-colors">
  <span className="text-2xl">🏷️</span>
  <span className="font-semibold text-gray-900 dark:text-white">Promociones</span>
  <span className="text-xs text-gray-500">Descuentos, cupones y ofertas</span>
</Link>
```

- [ ] **Step 3: Commit**

```bash
git add app/admin/page.tsx
git commit -m "feat: add promotions link to admin dashboard"
```
